# RocketRide engine connection — auto-managed by the VS Code extension.
ROCKETRIDE_URI=
ROCKETRIDE_APIKEY=

# Gemini API key for the judge LLM (get one at https://aistudio.google.com/apikey).
# This file holds PLACEHOLDERS only — never commit the real key. The real value
# goes in your local .env (gitignored) or the node's API-key field.
ROCKETRIDE_GEMINI_KEY=your-gemini-api-key-here
